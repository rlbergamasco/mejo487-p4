import React, { useState } from 'react';
import MapContainer from './MapContainer';

const App = () => {

  return (
    <React.Fragment>
      <MapContainer />
    </React.Fragment>
  );
}

export default App;
